# Các thay đổi chính cho npm-multi-publish

## 🎯 Mục tiêu

Điều chỉnh để:
1. ✅ Một source code nhưng publish lên nhiều host với package name khác nhau
2. ✅ Tách riêng source tgz cho từng host tránh xung đột
3. ✅ Tự động tăng version theo format: 1.yy.mmdd.1hhMM
4. ✅ Cập nhật NPX.md với các lệnh chạy trực tiếp

## 📝 Chi tiết thay đổi

### 1. File: `src/core/publisher.js`

#### Thêm method `generateVersion()`
```javascript
/**
 * Generate version: 1.yy.mmdd.1hhMM
 */
generateVersion() {
  const now = new Date();
  const yy = String(now.getFullYear()).slice(-2);
  const mm = String(now.getMonth() + 1).padStart(2, '0');
  const dd = String(now.getDate()).padStart(2, '0');
  const hh = String(now.getHours()).padStart(2, '0');
  const MM = String(now.getMinutes()).padStart(2, '0');
  
  return `1.${yy}.${mm}${dd}.1${hh}${MM}`;
}
```

**Ví dụ output:**
- Ngày 30/01/2026 lúc 15:45 → `1.26.0130.11545`
- Ngày 15/12/2026 lúc 09:30 → `1.26.1215.10930`

#### Thêm method `createRegistryPackage()`
```javascript
/**
 * Create package for specific registry
 */
async createRegistryPackage(registry, originalPkg) {
  const registryPkgPath = path.join(this.tmpDir, `package-${registry.name}.json`);
  const newVersion = this.generateVersion();
  
  // Clone package.json
  const registryPkg = { ...originalPkg };
  
  // Update version
  registryPkg.version = newVersion;
  
  // Update package name based on registry config
  if (registry.config.scope) {
    // Remove existing scope if any
    const baseName = registryPkg.name.replace(/^@[^/]+\//, '');
    registryPkg.name = `${registry.config.scope}/${baseName}`;
  }
  
  // Write registry-specific package.json
  fs.writeFileSync(registryPkgPath, JSON.stringify(registryPkg, null, 2));
  
  return { pkgPath: registryPkgPath, pkg: registryPkg };
}
```

**Chức năng:**
- Tạo package.json riêng cho từng registry
- Tự động cập nhật version mới
- Thay đổi package name theo scope của registry

**Ví dụ:**
```javascript
// Original package.json
{
  "name": "my-package",
  "version": "1.0.0"
}

// Registry: npmjs (no scope)
{
  "name": "my-package",
  "version": "1.26.0130.11545"
}

// Registry: codeberg (scope: @o861runners)
{
  "name": "@o861runners/my-package",
  "version": "1.26.0130.11545"
}

// Registry: github (scope: @myusername)
{
  "name": "@myusername/my-package",
  "version": "1.26.0130.11545"
}
```

#### Thêm method `packForRegistry()`
```javascript
/**
 * Pack for specific registry
 */
async packForRegistry(registry, pkgPath) {
  const spinner = ora(`Packing for ${registry.name}...`).start();
  
  try {
    // Backup original package.json
    const originalPkgPath = path.join(process.cwd(), 'package.json');
    const backupPkgPath = path.join(this.tmpDir, 'package.json.backup');
    fs.copyFileSync(originalPkgPath, backupPkgPath);
    
    // Replace with registry-specific package.json
    fs.copyFileSync(pkgPath, originalPkgPath);
    
    // Pack
    const packOutput = execSync('npm pack --json', { encoding: 'utf-8' });
    const packInfo = JSON.parse(packOutput)[0];
    const artifactPath = packInfo.filename;
    
    // Move to registry-specific directory
    const registryArtifactPath = path.join(this.tmpDir, `${registry.name}-${packInfo.filename}`);
    fs.renameSync(artifactPath, registryArtifactPath);
    
    // Restore original package.json
    fs.copyFileSync(backupPkgPath, originalPkgPath);
    
    spinner.succeed(`Package created for ${registry.name}`);
    console.log(chalk.gray(`   Name: ${packInfo.name}@${packInfo.version}`));
    console.log(chalk.gray(`   Size: ${(packInfo.size / 1024).toFixed(2)} KB`));
    console.log(chalk.gray(`   File: ${registryArtifactPath}`));
    
    return registryArtifactPath;
  } catch (error) {
    spinner.fail(`Pack failed for ${registry.name}`);
    throw error;
  }
}
```

**Chức năng:**
- Backup package.json gốc
- Thay thế tạm thời bằng package.json của registry
- Pack thành file .tgz riêng
- Đổi tên file theo registry
- Khôi phục package.json gốc

**Ví dụ output files:**
```
.npm-multi-publish-tmp/
├── npmjs-my-package-1.26.0130.11545.tgz
├── codeberg-o861runners-my-package-1.26.0130.11545.tgz
├── github-myusername-my-package-1.26.0130.11545.tgz
├── package-npmjs.json
├── package-codeberg.json
├── package-github.json
└── package.json.backup
```

#### Sửa đổi `run()` method
```javascript
async run() {
  // ... (build step)
  
  // 2. Read original package.json
  const originalPkg = JSON.parse(fs.readFileSync('package.json', 'utf-8'));
  console.log(chalk.bold(`\n📦 Original package: ${originalPkg.name}@${originalPkg.version}\n`));

  // ... (validate, authenticate)

  // 5. Create and publish to each registry
  console.log(chalk.bold('📦 Creating packages and publishing...\n'));
  const results = [];

  for (const registry of this.registries) {
    if (this.options.dryRun) {
      // ... dry run logic
    } else {
      try {
        // Create registry-specific package
        const { pkgPath, pkg } = await this.createRegistryPackage(registry, originalPkg);
        console.log(chalk.blue(`📝 Package name for ${registry.name}: ${pkg.name}@${pkg.version}`));
        
        // Pack for registry
        const artifactPath = await this.packForRegistry(registry, pkgPath);
        
        // Publish
        const result = await registry.publish(artifactPath);
        results.push({
          registry: registry.name,
          packageName: pkg.name,
          version: pkg.version,
          ...result,
        });

        if (result.success) {
          console.log(chalk.green(`✓ ${registry.name} - ${pkg.name}@${pkg.version}`));
        } else {
          console.log(chalk.red(`✗ ${registry.name}: ${result.error}`));
        }
      } catch (error) {
        results.push({
          registry: registry.name,
          success: false,
          error: error.message,
        });
        console.log(chalk.red(`✗ ${registry.name}: ${error.message}`));
      }
      
      console.log('');
    }
  }
  
  // ... (cleanup, summary)
}
```

### 2. File: `NPX.md` (Hoàn toàn mới)

Tạo file hướng dẫn chi tiết về cách chạy package trực tiếp từ các registry:

**Nội dung chính:**
- 🌐 NPM Registry commands
- 🐙 GitHub Packages commands  
- 🦎 Gitea/Codeberg commands
- 📦 Các lệnh hữu ích (view, versions, etc.)
- 🔧 Troubleshooting
- 💡 Tips (aliases, scripts, .npmrc)
- 📋 Quick Reference Table
- 📌 Examples với scenarios thực tế

**Các lệnh một dòng (One-liner):**
```bash
# NPM
npx --yes @o861runners-gif/simplepkg2 hello

# Codeberg
npx --yes --registry=https://codeberg.org/api/packages/o861runners/npm/ @o861runners-gif/simplepkg2 hello

# GitHub
npx --yes --registry=https://npm.pkg.github.com @username/package hello

# Gitea Custom
npx --yes --registry=https://git.domain.com/api/packages/owner/npm/ @owner/package hello
```

## 🔄 Flow mới khi publish

```
┌─────────────────────────────────────────────────────────────┐
│  1. Read original package.json                               │
│     - name: "my-package"                                     │
│     - version: "1.0.0"                                       │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  2. For each registry:                                       │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ a. Generate new version: 1.26.0130.11545             │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ b. Create registry-specific package.json             │  │
│  │    - npmjs:     my-package@1.26.0130.11545          │  │
│  │    - codeberg:  @o861runners/my-package@...         │  │
│  │    - github:    @myusername/my-package@...          │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ c. Backup original package.json                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ d. Replace with registry-specific package.json       │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ e. npm pack → create .tgz file                       │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ f. Rename: registry-name-package-version.tgz         │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ g. Restore original package.json                     │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ h. Publish .tgz to registry                          │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Kết quả sau khi publish

### Console Output
```
🚀 NPM Multi-Registry Publisher

📦 Original package: my-package@1.0.0

📋 Validating registries...

✓ npmjs
✓ codeberg
✓ github

🔑 Authenticating...

✓ npmjs
✓ codeberg
✓ github

📦 Creating packages and publishing...

📝 Package name for npmjs: my-package@1.26.0130.11545
⠋ Packing for npmjs...
✔ Package created for npmjs
   Name: my-package@1.26.0130.11545
   Size: 1.23 KB
   File: .npm-multi-publish-tmp/npmjs-my-package-1.26.0130.11545.tgz
📦 Publishing to npmjs...
   Registry: https://registry.npmjs.org
✓ npmjs - my-package@1.26.0130.11545

📝 Package name for codeberg: @o861runners/my-package@1.26.0130.11545
⠋ Packing for codeberg...
✔ Package created for codeberg
   Name: @o861runners/my-package@1.26.0130.11545
   Size: 1.23 KB
   File: .npm-multi-publish-tmp/codeberg-o861runners-my-package-1.26.0130.11545.tgz
📦 Publishing to codeberg...
   Registry: https://codeberg.org/api/packages/o861runners/npm
✓ codeberg - @o861runners/my-package@1.26.0130.11545

📝 Package name for github: @myusername/my-package@1.26.0130.11545
⠋ Packing for github...
✔ Package created for github
   Name: @myusername/my-package@1.26.0130.11545
   Size: 1.23 KB
   File: .npm-multi-publish-tmp/github-myusername-my-package-1.26.0130.11545.tgz
📦 Publishing to github...
   Registry: https://npm.pkg.github.com
✓ github - @myusername/my-package@1.26.0130.11545

📊 Summary:

✅ npmjs - my-package@1.26.0130.11545 [SUCCESS]
✅ codeberg - @o861runners/my-package@1.26.0130.11545 [SUCCESS]
✅ github - @myusername/my-package@1.26.0130.11545 [SUCCESS]

Total: 3 | Success: 3 | Failed: 0

✅ All registries published successfully!
```

## 🔍 Config Example

```json
{
  "registries": {
    "npmjs": {
      "type": "npm",
      "registry": "https://registry.npmjs.org",
      "token": "{{NPM_TOKEN}}",
      "access": "public",
      "enabled": true
    },
    "codeberg": {
      "type": "gitea",
      "url": "https://codeberg.org",
      "owner": "o861runners",
      "token": "{{CODEBERG_TOKEN}}",
      "access": "public",
      "enabled": true
    },
    "github": {
      "type": "github",
      "owner": "myusername",
      "repo": "myrepo",
      "token": "{{GITHUB_TOKEN}}",
      "enabled": true
    }
  }
}
```

**Kết quả:**
- npmjs: `my-package@1.26.0130.11545`
- codeberg: `@o861runners/my-package@1.26.0130.11545`
- github: `@myusername/my-package@1.26.0130.11545`

## ✅ Lợi ích

1. **Tách biệt hoàn toàn:** Mỗi registry có file .tgz riêng, tránh xung đột
2. **Package name linh hoạt:** Tự động thêm scope theo registry
3. **Version tự động:** Không cần tự tăng version, luôn unique
4. **Dễ debug:** Biết chính xác file nào publish lên registry nào
5. **An toàn:** Luôn backup và restore package.json gốc

## 🚀 Cách sử dụng

```bash
# 1. Setup environment
export NPM_TOKEN=npm_xxxxx
export CODEBERG_TOKEN=xxxxx
export GITHUB_TOKEN=ghp_xxxxx

# 2. Publish to all registries
npm-multi-publish publish

# 3. Test from each registry
npx --yes my-package hello
npx --yes --registry=https://codeberg.org/api/packages/o861runners/npm/ @o861runners/my-package hello
npx --yes --registry=https://npm.pkg.github.com @myusername/my-package hello
```

## 📝 Notes

- Version format đảm bảo mỗi lần publish đều có version mới và lớn hơn
- Số `1` cố định ở đầu và giữa để đảm bảo semver compatibility
- Cleanup tự động sau khi publish xong
- Có thể target specific registry: `--target npmjs,codeberg`
